// import useState
import { useState } from "react";

export default function UseStateUp() {
  
  return (
    <>
      <h1>{foo.title}</h1>
      <h1>{bar.title}</h1>
      <h2>{title.one}</h2>
      <button onClick={handleClick}>Click Me</button>
    </>
  );
}
